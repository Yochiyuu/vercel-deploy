generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Mahasiswa {
  id    Int    @id @default(autoincrement())
  nama  String
  nim   String
}